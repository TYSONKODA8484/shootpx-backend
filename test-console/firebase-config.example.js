// Copy this file to firebase-config.js and fill in your real values from
// Firebase Console -> Project settings -> General -> "Your apps".
// firebase-config.js itself is gitignored — this template is what's
// committed, same pattern as .env / .env.example in the backend root.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
};
